#DATABASE

#DataSource
spring.datasource.url=jdbc:mysql://localhost:3306/it_announcements_portal?autoReconnect=true&useSSL=false
spring.datasource.username=root
spring.datasource.password=Mz8jpg5a1
spring.datasource.driver-class-name=com.mysql.jdbc.Driver

#Hibernate
spring.jpa.hibernate.ddl-auto=update

spring.jpa.show-sql=true


#SPRING SECURITY
#spring.queries.users-query=select email, password, active from user, company where email=?
#spring.queries.roles-query=select u.email, c.email, r.role from user u inner join user_role ur on(u.user_id=ur.user_id) inner join company c inner join company_role cr on(c.company_id=cr.company_id) inner join role r on(cr.role_id=r.role_id) where u.email=? or c.email=?
#spring.queries.users-query=select email, password, active from company where email=?
#spring.queries.roles-query=select e.email, r.role from company e inner join company_role er on(e.company_id=er.company_id) inner join role r on(er.role_id=r.role_id) where e.email=?
